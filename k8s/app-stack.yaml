apiVersion: apps/v1
kind: Deployment
metadata:
  name: auy1104-app-deployment
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auy1104-app
  template:
    metadata:
      labels:
        app: auy1104-app
    spec:
      containers:
        - name: auy1104-app
          image: DOCKER_USERNAME_PLACEHOLDER/auy1104-app:latest
          ports:
            - containerPort: 3000
          env:
            - name: PORT
              value: "3000"
---
apiVersion: v1
kind: Service
metadata:
  name: auy1104-app-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: auy1104-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30090